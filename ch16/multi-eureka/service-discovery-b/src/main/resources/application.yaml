server:
  port: 8762

spring:
  application:
    name: service-discovery-b

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: host.docker.internal
  instance:
    hostname: dowon-thinkpad
